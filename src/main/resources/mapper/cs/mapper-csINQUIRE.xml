<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Intake2020.mapper.CsINQUIREMapper">
 
 	<insert id="write">
 		insert into CsINQUIRE(writer,phoneNum,subject,content,fileName)
 		values(#{writer},#{phoneNum},#{subject},#{content},#{fileName})
 	</insert>

</mapper>